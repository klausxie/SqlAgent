[project]
name = "database-tools-mcp"
version = "0.1.0"
description = "MCP server for database metadata and SQL analysis tools"
readme = "README.md"
requires-python = ">=3.12.2"
dependencies = [
    "mcp[cli]>=1.3.0",
    "pymysql>=1.1.0",
    "psycopg2-binary>=2.9.9",
    "sqlparse>=0.5.0",
]
